<%= form_with(model: fundamental_info, local: true) do |form| %>
  <% if fundamental_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fundamental_info.errors.count, "error") %> prohibited this fundamental_info from being saved:</h2>

      <ul>
      <% fundamental_info.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
